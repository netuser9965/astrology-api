# 正式表單頁版本

新增 `/form` 正式用戶表單頁。

Render 設定：
Build Command: pip install -r requirements.txt
Start Command: python -m uvicorn main:app --host 0.0.0.0 --port $PORT

WordPress 按鈕連結：
https://astrology-api-l5tr.onrender.com/form
